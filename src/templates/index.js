const { greetRep } = require('./greet');
const { incoRep } = require('./incotermRep');
const { locationRep } = require('./location');
const { scheduleRep } = require('./schedule');
const { socialRep } = require('./social');

module.exports = {
  incoRep,
  greetRep,
  scheduleRep,
  socialRep,
  locationRep,
};

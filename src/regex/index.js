const { greetPattern } = require('./greet');
const { incotermPattern } = require('./incoterm');
const { locationPattern } = require('./location');
const { schedulePattern } = require('./schedule');
const { socialPattern } = require('./social');

module.exports = {
  incotermPattern,
  greetPattern,
  schedulePattern,
  socialPattern,
  locationPattern,
};
